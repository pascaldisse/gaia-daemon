{
  "name": "gaia.defaults",
  "version": "1.0.0",
  "description": "gaia plugin gaia.defaults (migrated to a pi package by `gaia plugin migrate`)",
  "type": "module",
  "private": true,
  "pi": {
    "extensions": [
      "./pi-extension.mjs"
    ]
  }
}
