# Bundled add-ons

- source → manifest packages snapshotted beside compiled daemon
- current boundary → metadata validation only · import/register/invoke disabled
- runtime activation → future lifecycle-host atom + compiled/live gate
