# Image cap · 2026-09-06

§ Cause
Nyari attachment: PNG 9,137,638 B → base64 12,183,520 B > provider 10,485,760 B.
`core/attachments.ts:loadNativeImages` → unbounded original bytes; compaction → retained oversized image → repeated400.

§ Fix
`core/image.ts` → shared lazy Photon loader + encoder; base64 cap parameter/default4.5MiB (existing cross-provider read budget); PNG first → JPEG80/70/55/40 → resize only if necessary.
`core/attachments.ts` → bounded provider copy; MIME follows encoding; original file/path preserved.
`core/image-request.ts` → content-only wire normalization + bounded hash cache; Messages/Chat/Responses/Gemini inline images; nested tool-result content; arguments/text/remote URLs untouched.
`harness/runner.ts` + `services/proxy.ts` → same normalizer; zero harness-id branches; retained history protected without session rewrites.
Oversized undecodable wire image → local error, never resend known-over-cap bytes.

§ Gates
`bun run check` → EXIT0.
Separate `bun test test/<name>.test.ts` → image-request7 + image-proxy1 + gaia-tool14 + pi-runtime65 + runner-host-proxy2 + runner-plugins5 = 94pass/0fail.
`bun scripts/build-daemon.mjs --out <owned-proof>/build` → compiled binary + assets PASS.
Independent Opus review → no blocking regressions.

§ Real daemon/provider proof · isolated home/workspace · :19879
Exact failing PNG → actual upload route → room message → compiled runner → Anthropic Sonnet5 OAuth.
Provider copy: JPEG base64 1,444,120 B; source/upload SHA256 both `c2d129c4cbd4d0c8c487c05cdaa3064086ca9dd326496fae837da09bbab73699`.
Reply: `IMAGE-CAP-LIVE-OK` + correct prehistoric game / citizen gathering wood description.
Real bash `gaia summon ghoul-sonnet "Reply IMAGE-CAP-SUMMON-OK only; no tools."` → child `ghoul-sonnet-mtq9jmoddn81kr` → `IMAGE-CAP-SUMMON-OK`.
Isolated historical-session fixture: restored original 12,183,520-B image block → fresh runner → `IMAGE-CAP-HISTORY-OK — Prehistoric Age`.
Final compiled build / fresh owned runner → `IMAGE-CAP-FINAL-OK — Prehistoric Age; unit action: gather (5 wood)`.
Post-request historical block still 12,183,520 B → wire-only repair confirmed.
Owned daemon stopped; :19879 empty. Pascal app + original Nyari session/attachments untouched.

§ Evidence / landing
Room worktree `.gaia/worktrees/chat-mtq974eg-jdk9`; local archive within worktree `.gaia/image-cap-proof/` → proof.json, *-final.log, live-daemon.log, original-hashes.txt, live-ws/.gaia/rooms/.
Archive moved from worktree `.scratch/` after shutdown; historical log paths retain that prefix.
Production: Pascal-authorized normal rebuild → installed clean acb7ba9, built 2026-09-06T20:48:37Z; supervisor status verified20:49Z; actual Nyari reply `evt_mtqaayka_zfiqygp1` → correct screenshot description, images readable.
Deployment evidence → worktree `.gaia/image-cap-deploy/status.json`; sourceRoot still this worktree → preserve until source-pointer migration; no second rebuild.
Landing: Pascal22:54 authorized preserving unrelated root `proof/` → root `.gaia/preserved-proof/`; no deletion. Main merged into room branch → already current; fresh check + six targeted test files → 94pass/0fail. Logs → `.gaia/image-cap-deploy/merge-*.log`.
Nyari Fly brain/Atlas entries → pending, outside image-recovery scope.
