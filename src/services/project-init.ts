/** Model prompt behind `/init`; the transcript deliberately retains only `/init`. */
export const PROJECT_INIT_PROMPT = `Initialize this project strategically. Recursively inspect the repository and its existing guidance, then maintain AGENTS.md without discarding existing useful instructions. Exclude secrets, generated files, build output, dependency directories, and other non-source artifacts from inspection and documentation. Write compact telegraphic context: repository map, architecture, commands, conventions, risks, and durable working rules. Do not expose secret contents. Make only justified updates; report what you changed.`;
