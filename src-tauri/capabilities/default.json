{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for the main window. The gaia UI talks to its own localhost server, not to Tauri IPC, so only core defaults are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
